{
  "name": "wave",
  "version": "1.0.0",
  "description": "Chrome extension for hands-free recipe scrolling with hand gestures",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.18",
    "canvas": "^3.2.2",
    "sharp": "^0.34.5"
  }
}
